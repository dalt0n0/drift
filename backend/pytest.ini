[pytest]
asyncio_mode = auto
markers =
    e2e: end-to-end tests requiring live Drift stack and target containers

# Default: exclude E2E tests. Run them explicitly with:
#   pytest tests/e2e -m e2e --no-header -v
addopts = --ignore=tests/e2e
